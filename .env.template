# ===================
# .env.template 
# ===================
# Environment Configuration Template
# FOR LOCAL DEVELOPMENT:
# 1. Copy this file: cp .env.template .env
# 2. Fill in your actual values in .env
# 3. NEVER commit the .env file to Git
#
# FOR RENDER DEPLOYMENT:
# Set these as environment variables in Render dashboard
# (Do not upload .env file to production)

# REQUIRED: Google Gemini AI API Key
GEMINI_API_KEY=your_gemini_api_key_here

# REQUIRED: Service API Key for client authentication
# Generate with: openssl rand -base64 32
SERVICE_API_KEY=your_secure_api_key_here

# OPTIONAL: Redis URL for rate limiting (if not set, uses in-memory fallback)
# REDIS_URL=redis://localhost:6379/0

# Application Configuration
ENV=production
PORT=8000
GEMINI_MODEL=gemini-1.5-flash
LLM_TEMPERATURE=0.3

# Security & Rate Limiting
RATE_LIMIT_DEFAULT=30/minute
MAX_CONTENT_LENGTH=524288
MAX_QUESTION_LENGTH=2000
LLM_TIMEOUT_SECONDS=30
MAX_RETRY_ATTEMPTS=3

# CORS Configuration (comma-separated origins)
# Include your actual frontend URLs here
FRONTEND_ORIGINS=https://your-vercel-app.vercel.app,https://your-custom-domain.com

# Logging Level
LOG_LEVEL=INFO